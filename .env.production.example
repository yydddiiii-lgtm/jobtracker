# 复制此文件为 .env，然后填入真实值
# cp .env.production.example .env

# 数据库用户名（可自定义）
DB_USER=jobtracker

# 数据库密码（必填，请设置强密码）
DB_PASSWORD=请替换为强密码_至少16位

# JWT 密钥（必填，至少32字符）
# 生成命令：openssl rand -base64 32
JWT_SECRET=请替换为随机密钥_至少32字符

# JWT 过期时间（可保持默认）
JWT_ACCESS_EXPIRES_IN=2h
JWT_REFRESH_EXPIRES_IN=7d
